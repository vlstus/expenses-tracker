package org.stus.tracker.repository.user;

import org.stus.tracker.model.user.User;
import org.stus.tracker.repository.EntityRepository;

public interface UserRepository extends EntityRepository<User, Long> {

}
